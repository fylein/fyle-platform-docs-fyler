type: object
properties:
  count:
    allOf:
      - $ref: '../commons/count.yaml'
    example: 1
  offset:
    $ref: '../commons/offset.yaml'
  limit:
    $ref: '../commons/limit.yaml'
  url:
    type: string
    example: '/v4/fyler/orgs'
  data:
    type: array
    items:
      $ref: '../../objects/orgs.yaml'