# Fyler API Documentation #

## Prerequisites ##

Install swagger-cli - this will allow you to combine 

    npm install -g swagger-cli

## Building ##

    swagger-cli bundle -o fyler.yaml -t yaml -r main.yaml

